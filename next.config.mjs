/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/0617CCTV-HOMEPAGE",
  trailingSlash: true,
  images: { unoptimized: true },
};

export default nextConfig;
